            let deffValue = getTimeDifference(endtime),
                size = Object.keys(deffValue).length;

            for (let i = 1; i < size; i++){
                i;
                // if (deffValue[i] < 10) {
                //     deffValue[i] = '0' + deffValue[i];
                // }
            }
            console.log(size);